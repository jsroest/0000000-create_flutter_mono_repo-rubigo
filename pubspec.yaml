name: create_flutter_mono_repo
description: Create a Flutter mono repository for Android Studio
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  args: ^1.6.0
  cli_util: ^0.2.0
  path: ^1.7.0
  process_run: ^0.11.2+8
  meta: ^1.2.4
  xml: ^4.5.1

dev_dependencies:
  pedantic: ^1.9.0
